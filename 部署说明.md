# 个人专属工作台 · 部署说明

这套文件是一个标准 PWA（可像 APP 一样「安装」到手机主屏）。
部署到任意**静态托管**后，就会得到一个**永久 HTTPS 链接**，把它「添加到主屏幕」即为 APP。

## 包含文件
- `index.html`      主页面（全部功能逻辑）
- `manifest.json`   PWA 清单（让浏览器识别为可安装 APP）
- `sw.js`           Service Worker（秒开 + 离线可用）
- `icon-192.png` / `icon-512.png`  主屏图标

## 方式一：GitHub Pages（推荐，免费、永久、最简单）
1. 打开 https://github.com/new 新建一个仓库（名字随意，如 `workstation`，选 **Public**）。
2. 把上面 5 个文件**直接拖进**网页上传，或本地 `git push`。
3. 仓库 → **Settings → Pages → Build and deployment → Source 选 `Deploy from a branch` → Branch 选 `main` / `master`**。
4. 等 1 分钟，访问 `https://<你的用户名>.github.io/<仓库名>/` 即为永久链接。

## 方式二：Netlify / Vercel（拖拽即上线）
- Netlify：打开 https://app.netlify.com/drop ，把 5 个文件拖进去即可，立刻拿到永久链接。
- Vercel：新建项目 → 上传文件夹 → 自动部署。

## 方式三：阿里云 OSS / 腾讯云 COS（国内访问更快）
- 建一个 Bucket，开启「静态网站托管」，把 5 个文件传上去，绑定自有域名 + 免费 HTTPS 证书即可。

## 装成 APP
- **安卓**：用 Chrome 打开链接 → 右上角 `⋮` → **「安装应用」**。
- **iPhone**：用 **Safari** → 分享 → **「添加到主屏幕」**（注意别用 Chrome）。
- 之前那个带地址栏的旧图标请**长按删除后重加**。

> 提示：`manifest.json` / `sw.js` / 图标必须是**真实文件**（不能内联），这正是本包的结构，部署后即可正常「安装」。
